# ============================================================
# JagoIndia Backend Environment Configuration
# ============================================================

# Server Configuration
PORT=5000
NODE_ENV=production

# Frontend URL (for CORS and redirects)
FRONTEND_URL=https://jagoindia.com

# MongoDB Atlas Connection
# Format: mongodb+srv://username:password@cluster0.uniqueid.mongodb.net/database
MONGO_URI=mongodb+srv://username:password@your-cluster.mongodb.net/jagoindia

# JWT Configuration
JWT_SECRET=your_super_secret_jwt_key_change_in_production_min_32_chars
JWT_EXPIRES_IN=7d

# Admin Credentials (used for initial setup only)
ADMIN_NAME=AMIT RAJWAR
ADMIN_EMAIL=amitxrajwar@gmail.com
ADMIN_PASSWORD=your_secure_password_change_in_production

# ============================================================
# Keep-Alive Configuration (CRITICAL for Render Free Tier)
# ============================================================
# Render free tier services sleep after 15 minutes of inactivity
# This keep-alive system pings the /health endpoint periodically
# to prevent the service from sleeping
#
# HOW TO USE:
# 1. Deploy to Render
# 2. Copy your Render service URL (e.g., https://jagoindia-api.onrender.com)
# 3. Set SELF_PING_URL to: https://YOUR-SERVICE-URL/health
# 4. Set ENABLE_SELF_PING=true
# 5. The app will automatically ping itself every 10 minutes
#
# EXAMPLE for Render:
# ENABLE_SELF_PING=true
# SELF_PING_URL=https://jagoindia-api-abc123.onrender.com/health
# SELF_PING_INTERVAL=600000

# Enable/disable the self-ping keep-alive mechanism
# true = enabled (keeps service awake on Render)
# false = disabled
# Default: true in production (if SELF_PING_URL is set)
ENABLE_SELF_PING=true

# Full URL to the /health endpoint for keep-alive pings
# REQUIRED in production: https://YOUR-RENDER-SERVICE-URL/health
# Use the exact URL from your Render dashboard
# The app will make GET requests to this URL every SELF_PING_INTERVAL ms
SELF_PING_URL=https://jagoindia-api.onrender.com/health

# Interval between keep-alive pings in milliseconds
# Default: 600000 (10 minutes)
# Render sleep threshold: ~15 minutes
# Recommendation: 600000 (10 min) or 540000 (9 min) for safety
SELF_PING_INTERVAL=600000

# Enable verbose logging for keep-alive pings (dev debugging only)
# true = log every ping attempt
# false = only log errors (recommended for production)
KEEP_ALIVE_VERBOSE=false
